import { StyleSheet, Text, View } from "react-native";
import React from "react";

const StrapiModel = () => {
  return (
    <View>
      <Text>StrapiModel</Text>
    </View>
  );
};

export default StrapiModel;

const styles = StyleSheet.create({});
