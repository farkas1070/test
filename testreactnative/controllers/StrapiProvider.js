import { StyleSheet, Text, View } from 'react-native'
import React from 'react'

const StrapiProvider = () => {
  return (
    <View>
      <Text>StrapiProvider</Text>
    </View>
  )
}

export default StrapiProvider

const styles = StyleSheet.create({})